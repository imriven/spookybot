/** @private */
export declare function createExchangeCodeQuery(clientId: string, clientSecret: string, code: string, redirectUri: string): {
    grant_type: string;
    client_id: string;
    client_secret: string;
    code: string;
    redirect_uri: string;
};
/** @private */
export declare function createGetAppTokenQuery(clientId: string, clientSecret: string): {
    grant_type: string;
    client_id: string;
    client_secret: string;
};
/** @private */
export declare function createRefreshTokenQuery(clientId: string, clientSecret: string, refreshToken: string): {
    grant_type: string;
    client_id: string;
    client_secret: string;
    refresh_token: string;
};
/** @private */
export declare function createRevokeTokenQuery(clientId: string, accessToken: string): {
    client_id: string;
    token: string;
};
//# sourceMappingURL=helpers.external.d.ts.map