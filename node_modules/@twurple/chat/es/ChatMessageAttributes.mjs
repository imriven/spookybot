import { TwitchPrivateMessage } from "./commands/TwitchPrivateMessage.mjs";
/** @private */
export function extractMessageId(message) {
    return message instanceof TwitchPrivateMessage ? message.tags.get('id') : message;
}
