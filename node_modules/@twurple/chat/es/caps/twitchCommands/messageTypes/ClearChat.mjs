import { __decorate } from "tslib";
import { Message, MessageParamDefinition, MessageType } from 'ircv3';
let ClearChat = class ClearChat extends Message {
    get date() {
        const timestamp = this._tags.get('tmi-sent-ts');
        return new Date(Number(timestamp));
    }
    get channelId() {
        return this._tags.get('room-id');
    }
    get targetUserId() {
        var _a;
        return (_a = this._tags.get('target-user-id')) !== null && _a !== void 0 ? _a : null;
    }
};
__decorate([
    MessageParamDefinition({
        type: 'channel'
    })
], ClearChat.prototype, "channel", void 0);
__decorate([
    MessageParamDefinition({
        trailing: true,
        optional: true
    })
], ClearChat.prototype, "user", void 0);
ClearChat = __decorate([
    MessageType('CLEARCHAT')
], ClearChat);
export { ClearChat };
